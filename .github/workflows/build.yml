name: build

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            asset: BiedaOS.exe
            built: dist/BiedaOS.exe
          - os: macos-latest
            asset: BiedaOS-mac
            built: dist/BiedaOS
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install . pyinstaller pytest httpx
      - run: pytest -q
      - run: pyinstaller BiedaOS.spec
      - if: matrix.os == 'macos-latest'
        run: mv dist/BiedaOS dist/BiedaOS-mac
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: dist/${{ matrix.asset }}
      - if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/${{ matrix.asset }}
